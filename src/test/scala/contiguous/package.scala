package object contiguous {
  /*
   * A package containing property tests showing that you can make general property assertions on the projection
   * functions of contiguous periods, you only need to write these projections and tests once, then for all
   * possible system states the constraints you need will hold. This means when writing operations in production
   * that can affect the state you do not need to worry about validating these constraints.
   */
}
